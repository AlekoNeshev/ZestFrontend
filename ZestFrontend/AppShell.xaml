<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ZestFrontend.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ZestFrontend"
    Shell.FlyoutBehavior="Disabled">
    <TabBar>
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:Login}"/>
        <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:PostsPage}" />
        <ShellContent
        Title="Coms"
        ContentTemplate="{DataTemplate local:CommunitiesPage}" />
        <ShellContent
        Title="Account"
        ContentTemplate="{DataTemplate local:AccountPage}"/>
        <ShellContent
        Title="Friends"
        ContentTemplate="{DataTemplate local:FriendsPage}" />
    </TabBar>

</Shell>
